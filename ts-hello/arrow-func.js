// Long way to define a function
var log = function (message) {
    console.log(message);
};
// Shorter Way (One-line code can ommit {})
var doLog = function (message) { return console.log(message); }; // C# called LAMDA; Called Arrow in TypeScript
// No parameters
var doEmptyLog = function () { return console.log(); };
